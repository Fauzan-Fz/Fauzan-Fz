name: Generate snake animation

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run snake animation generator
        run: |
          # Your snake animation generation commands here

      - name: Push dist directory to output branch
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dist
          git commit -m "Generate snake animation"
          git push --force https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/Fauzan-Fz/Fauzan-Fz.git output
        env:
          GITHUB_TOKEN: ${{ secrets.PROFILE_TOKEN }}
